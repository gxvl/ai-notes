import { ButtonProps } from "./button";

type LoadingButtonProps = {
    loading: boolean,
} & ButtonProps

export default function loadingButton({
    children,
    loading,
    ...props
}: LoadingButtonProps) {
    return (
        
    )

}